import 'package:flutter/material.dart';

class AuthRegister extends StatelessWidget {
  const AuthRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("hi register"));
  }
}

import 'package:flutter/material.dart';

class AppInfoSettingsScreen extends StatelessWidget {
  const AppInfoSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
